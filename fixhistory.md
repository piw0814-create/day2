## 코드 작성 중 발생한 오류 및 수정 사항


Pandas와 Polars 데이터 크기 불일치

    Polars가 파일 마지막의 빈 줄을 하나의 행으로 인식해 데이터 크기가 달라졌다. 모든 값이 비어 있는 행을 제거하도록 수정하여 두 라이브러리의 결과를 (32561, 15)로 맞췄다.

문자열 전처리 과정의 경고

    Pandas에서 문자열 열을 처리할 때 경고가 발생해, 문자열 자료형 열만 선택한 후 공백을 제거하도록 수정했다.

보고서 해석이 고정된 문제

    처음에는 분석 결론을 문자열로 미리 작성했지만, 데이터가 바뀌어도 같은 해석이 출력될 수 있었다. 평균값, p-value, 상관계수, Precision과 Recall을 실제로 비교해 결론을 자동 생성하도록 수정했다.

시각화 함수가 None을 반환하는 오류

    Plotly 코드 수정 중 중복 코드가 생기고 반환문이 빠져 전체 Pipeline에서 오류가 발생했다. 중복된 박스플롯 코드를 제거하고 생성된 파일 경로를 딕셔너리로 반환하도록 수정했다.

Plotly HTML이 실행할 때마다 변경되는 문제

    HTML 내부 ID가 매번 새로 만들어져 Git에서 계속 수정 파일로 표시됐다. 고정된 div_id를 설정해 같은 데이터에서는 동일한 파일이 생성되도록 개선했다.
    프로젝트 구조 개선
    처음부터 하나의 파일에 모든 기능을 작성하지 않고, 데이터 로딩·전처리·EDA·통계·시각화·모델링·보고서 생성을 각각 분리했다. 덕분에 오류가 발생한 위치를 찾고 수정하기 쉬웠다.


이후 '팀원_비교_개선사항.md' 정리 후, 개선 사항

    Pandas·Polars 로딩 시간·메모리·dtype 비교
    비교 결과를 전체 Pipeline과 보고서에 연결
    income_binary와 수치형 변수의 상관관계
    Cohen’s d
    다수 클래스 베이스라인