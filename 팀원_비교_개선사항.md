## 1번 팀원(1조_손경락)

분석과 코드 선택의 이유를 주석과 설명에 적극적으로 드러냄

시각화 → 통계 검정 → 머신러닝을 하나의 분석 질문으로 연결
IQR 이상치를 무조건 삭제하지 않고 데이터 손실 비율을 보고 처리 여부 결정
다운로드 실패, 깨진 CSV 등 실패 상황 테스트
저장한 모델을 다시 불러와 예측 결과가 같은지 확인
RandomForest 변수 중요도 확인
max_depth를 제한하여 모델 크기 고려

핵심적으로 배울 점:
정상 실행만 확인하지 않고, 선택 근거와 실패 상황까지 검증한 것.

## 2번 팀원 (1조_성재원)

통계 결과와 모델 성능을 더 깊이 해석

income을 0과 1로 변환해 각 수치형 변수와 소득의 상관계수 계산
p-value뿐 아니라 Cohen’s d로 실제 차이의 크기 측정
평균뿐 아니라 중앙값과 사분위수도 함께 해석
모든 사람을 저소득으로 예측하는 단순 베이스라인과 모델 정확도 비교
모델 저장 후 재로딩·예측 일치 확인
결과 파일이 실제로 생성되었는지 마지막에 검사
실행 단계별 예외 처리

핵심적으로 배울 점:
“차이가 있는가?”뿐 아니라 “그 차이가 얼마나 크고, 모델이 단순 기준보다 얼마나 나은가?”까지 확인한 것.

## 3번 팀원(1조_강지수)

Pandas·Polars 비교와 모델 평가 지표를 확장.

Pandas·Polars 로딩 시간 비교
두 라이브러리의 메모리 사용량 비교
자료형 해석 차이 비교
ROC-AUC 평가 추가
max_depth=15로 모델 복잡도 제한
Plotly 박스플롯에서 데이터는 유지하고 이상치 점만 숨김
팀원별 역할과 공통 분석축을 README에 명시

핵심적으로 배울 점:
Pandas와 Polars를 단순히 둘 다 불러오는 데서 끝내지 않고, 성능과 메모리까지 비교 대상으로 확장한 것.

## 4번 팀원(1조_김다은)

비교 실험을 실제 실행 흐름과 보고서에 가장 직접적으로 연결.

동일한 로컬 파일을 대상으로 Pandas와 Polars 로딩 시간 측정
timeit으로 두 라이브러리를 동일한 횟수만큼 반복 측정
총시간과 평균시간을 보고서에 기록
학습 정확도와 테스트 정확도를 비교해 과적합 가능성 확인
max_depth=12 적용 전후 모델 파일 크기 차이 고려
화면이 없는 환경에서도 실행되도록 Matplotlib Agg 백엔드 적용
adult.test의 소득값 뒤 마침표까지 정리할 수 있게 처리

핵심적으로 배울 점:
성능 비교를 단발성 출력으로 끝내지 않고 전체 실행과 보고서에 포함한 것.

다만 Pandas와 Polars의 행 수가 다른 상태에서 속도를 비교


## 내 프로젝트의 강점

기능별 src 모듈 분리
run_pipeline.py는 전체 순서만 제어
config.yaml을 통한 설정 관리
원본 데이터 Git 제외 및 자동 다운로드
Pandas·Polars의 행 수와 컬럼 구조 일치 검증
결측값을 Unknown으로 보존해 데이터 손실 최소화
Welch t-test 사용
Precision, Recall, F1, 혼동행렬 평가
실행 결과에 따라 달라지는 동적 보고서
pytest와 Ruff
고정 div_id를 통한 Plotly HTML 재현성
매우 작은 p-value를 0.0000이 아닌 적절한 형식으로 표현

## 개선사항

1순위: 과제 요구를 더 직접적으로 충족

    Pandas·Polars 비교 확장

        현재는 크기와 컬럼만 비교했으므로 다음을 추가하는 것이 가장 중요.

            동일한 로컬 파일 사용
            동일한 반복 횟수로 로딩 시간 측정
            평균 로딩 시간 계산
            메모리 사용량 비교
            자료형 비교
            비교 결과를 자동 보고서에 포함

        비교 전에 두 결과가 동일한 (32561, 15)인지 먼저 확인

        Polars의 scan_csv()와 collect()를 사용하는 Lazy 방식도 검토.


2순위: 통계 분석 해석 강화

    목표 변수와 직접 연결된 상관관계

        현재는 수치형 변수끼리의 상관관계를 계산.

        다음을 추가하면 분석 목적에 더 직접적으로 연결.

            <=50K → 0
            >50K  → 1

        그 후 income_binary와 각 수치형 변수의 상관계수를 계산하는 방식

    Cohen’s d 추가

        현재 t-test는 차이의 존재 여부를 확인.

            p-value: 차이가 통계적으로 존재하는가
            Cohen’s d: 그 차이가 실제로 어느 정도 큰가

        표본이 매우 크기 때문에 작은 차이도 유의하게 나올 수 있어, 효과 크기를 함께 보는 것이 좋음.

    평균·중앙값·사분위수 비교

        소득 그룹별 근무시간에서 다음을 함께 계산

            평균
            중앙값
            25% 지점
            75% 지점

        그러면 평균 차이가 전체 분포의 차이인지, 일부 긴 근무시간 때문에 발생한 것인지 더 구체적으로 설명할 수 가능.

3순위: 모델 평가 강화

    다수 클래스 베이스라인

        현재 데이터의 약 76%가 <=50K야.

        따라서 모든 데이터를 저소득으로 예측해도 정확도가 약 76% 나온다는 기준과 RandomForest 성능을 비교.

            다수 클래스 기준 정확도
            vs
            RandomForest 정확도

        이렇게 해야 정확도 85.65%가 실제로 어느 정도 개선인지 알 수 있음.

    ROC-AUC 추가

        현재 지표는 유지하면서 ROC-AUC를 추가.

            Accuracy
            Precision
            Recall
            F1
            Confusion Matrix
            ROC-AUC

        ROC-AUC는 특정 임계값 하나가 아니라 여러 임계값에서 모델이 두 클래스를 얼마나 잘 구분하는지 보여줌.

    학습·테스트 성능 비교

        다음을 함께 기록하면 과적합 여부를 점검.

            학습 Accuracy
            테스트 Accuracy
            두 값의 차이

        Accuracy뿐 아니라 학습·테스트 F1도 비교하면 더 정확.

4순위: 모델 크기와 하이퍼파라미터 비교

    현재 모델은 max_depth=None

    특정 값을 바로 정답으로 적용하지 말고 다음처럼 실험하는 게 좋음.

        max_depth=None
        max_depth=10
        max_depth=15
        max_depth=20

    각 설정에서 비교할 항목은:

        학습 시간
        학습 Accuracy
        테스트 Accuracy
        Precision
        Recall
        F1
        ROC-AUC
        모델 파일 크기

    max_depth는 코드에 직접 쓰지 않고 config.yaml에서 관리하는 게 좋음.

        model:
        n_estimators: 100
        max_depth: null

5순위: 모델 해석과 저장 검증

    변수 중요도 추가

    RandomForest가 어떤 변수를 많이 활용했는지 확인할 수 있음.

    다만 보고서에서는 다음처럼 표현.

        중요도가 높다는 것은 모델 분할 과정에서 많이 활용됐다는 의미이며, 해당 변수가 소득의 직접적인 원인이라는 의미는 아니다.

        기본 변수 중요도보다 Permutation Importance가 해석상 더 적절할 수 있다.

    모델 재로딩 검증

    현재는 모델 파일을 저장하지만, 다시 읽어서 검증하지는 않음.

    학습된 Pipeline 예측

        → joblib 저장
        → 다시 로딩
        → 같은 입력으로 예측
        → 저장 전후 예측 결과 비교

    예측이 같아야 저장이 정상적으로 완료된 것으로 볼 수 있음.

6순위: 테스트와 실행 안정성

    현재 테스트는 정상적인 로딩과 전처리를 중심으로 작성. 다음을 추가하면 좋음.

        다운로드 실패
        존재하지 않는 파일
        잘못된 CSV 형식
        필수 컬럼 누락
        빈 DataFrame
        소득 그룹이 하나만 존재하는 데이터
        모델 저장 후 재로딩
        주요 결과 파일 생성 여부

    모든 오류를 except Exception으로 숨기기보다, 예상 가능한 예외만 구체적으로 처리하는 방식이 좋다.

## 최종 개선 우선순위

    우선순위

        먼저 보완

            Pandas·Polars 로딩 시간·메모리·dtype 비교
            비교 결과를 전체 Pipeline과 보고서에 연결
            income_binary와 수치형 변수의 상관관계
            Cohen’s d
            다수 클래스 베이스라인

        다음 단계로 추가

            ROC-AUC
            학습·테스트 성능 차이
            저장 모델 재로딩 검증
            모델 파일 크기 자동 기록
            실패 상황 테스트

        비교 실험으로 확장

            max_depth별 성능·용량 비교
            변수 중요도 또는 Permutation Importance
            class_weight 적용 전후 비교
            다른 분류 모델과 성능 비교
